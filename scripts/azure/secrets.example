# Copy this file to instances/NAME.local/secrets.env and fill in local-only values.
# Private instance profile directories are ignored by Git.
#
# Account credentials are normally loaded from the accounts.json in the same
# profile directory, so do not duplicate them here.

export OPENAI_API_KEY="KEY_VALUE"
