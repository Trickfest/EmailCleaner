# Copy this file to scripts/azure/secrets.local and fill in local-only values.
# scripts/azure/secrets.local is ignored by git through the repo's *.local rule.
#
# Account credentials are normally loaded from the ignored repo-root
# accounts.json file, so do not duplicate them here unless you intentionally
# want file-based overrides.

export OPENAI_API_KEY="KEY_VALUE"
